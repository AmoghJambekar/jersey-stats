# Build stage
FROM golang:1.25-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /api ./cmd/api

# Run stage
FROM alpine:3.21
COPY --from=build /api /api
EXPOSE 8080
CMD ["/api"]
