// Package httpapi will host Chi routes and middleware (docs/ROADMAP.md §10).
package httpapi

// NewRouter is a placeholder until Chi + sqlc handlers land.
func NewRouter() {
	// GET /health, GET /ready, /api/v1/* ...
}
