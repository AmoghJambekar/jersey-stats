# Copy to .env and fill in locally (never commit .env)
# DATABASE_URL=postgres://user:pass@localhost:5432/jerseystats?sslmode=disable
# BALLDONTLIE_API_KEY=...  # https://app.balldontlie.io/ — required for cmd/brunson-stats (stats tier)
